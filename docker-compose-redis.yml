
#
# sudo docker compose -f docker-compose-redis.yml up
#
#
services:
  redis:
    image: redis:latest
    container_name: redis-local
    ports:
      - "6379:6379"
    volumes:
      - ./resourcesRedis:/data:Z
    restart: unless-stopped

